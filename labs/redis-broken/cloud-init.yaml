#cloud-config
packages:
  - redis-server

runcmd:
  # Intentional break: add an invalid bind directive
  - sed -i 's/^bind.*/bind 256.256.256.256/' /etc/redis/redis.conf
  - systemctl restart redis-server || true
